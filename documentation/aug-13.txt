Date: August 13, 2026

Keys should remain the same, committing from 
different device so I had to re-add the env. 
If for some reason supabase operations aren't working, 
it could be new keys, but shouldn't be.

Current status: the contracts table is mainly storing 
metadata about contract start date, type of contract, 
end date, billing cycle length, if it is PIF or not, 
renewal date, etc. The packages table is redundant with 
a lot of this information, but it is purposed to serve a
s a blueprint for packages that can be assigned to any 
client. I will need to assess if it should remain 
redundant and optimize its purpose. The person 
packages table is currently disconnected but is 
intended to be the consumable services tied to a payment
a timeframe and client / trainer actions (like completing 
a session)

Upon further reflection, the packages table essentially 
serves as a template for contracts, while contracts 
represent actual instances. That is why there are duplicated 
fields. Perhaps some of that can be refined to just existing 
in packages, in other words contracts gets slimmed down. 
But no reason to currently. 

I am thinking that person packages should be precisely, 
and only, entitlements to services. 
Payments made under a specific contract should then trigger a row
creation. This table doesn't need to reference packages, 
just contracts. 


going to add backend failsafe for insufficient payments 


person packages seems sufficient... 
going to have to test out this flow with edge cases, and update the UI. 

person packages will also be the source of truth for included 
consumables. 

payments should only reference a contract right?

---
Schema sync (helpers updated Aug 13/14 2026):
Table names in the schema dump ARE included as top-level keys:
payments, contracts, person_packages, packages.

Notable column renames / changes reflected in fetch+upsert helpers:
- packages/contracts: cycle_length_weeks -> bill_cycle_length_weeks;
  "until cancelled" -> until_cancelled;
  default_cost_per_cycle -> cost_per_bill_cycle (contracts only; packages no longer store recurring cost)
- packages: dropped package_length_weeks, is_active, notes; added renewal_date, start_date, person_id, trainer_id
- person_packages: dropped start_date/end_date; added payment_date, next_payment_date, payment_id, contract_id
- payments: dropped billing_period_*; added return_payment_or_no; contract_id required

